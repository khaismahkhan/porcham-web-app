import { Typography } from "@mui/material";
import React from "react";
import { Button } from "react-bootstrap";

const Home = () => {
  return (
    <div>
      <Typography className="text-center text">Hello im material ui</Typography>
      <Button>click me</Button>
    </div>
  );
};

export default Home;
